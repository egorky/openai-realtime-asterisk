# rename this to .env

# Base URL for the websocket-server.
# The webapp will use this to connect to the /logs WebSocket endpoint (e.g., ws://localhost:8081/logs)
# and to fetch tools (e.g., http://localhost:8081/tools).
# IMPORTANT: If the webapp and websocket-server are running on different machines,
# or if the websocket-server is not accessible via 'localhost' from the machine
# running the webapp, you MUST change 'localhost' to the actual network IP address
# (or resolvable hostname) of the machine running the websocket-server.
# For example: NEXT_PUBLIC_WEBSOCKET_SERVER_BASE_URL="http://192.168.1.100:8081"
NEXT_PUBLIC_WEBSOCKET_SERVER_BASE_URL="http://localhost:8081"
